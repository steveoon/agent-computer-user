# Note: Removed node-linker=hoisted because it breaks electron module resolution
# Electron's require('electron') returns undefined when hoisted
# Will need to find alternative solution for electron-builder

# Enable build scripts for Electron and related packages
enable-pre-post-scripts=true
